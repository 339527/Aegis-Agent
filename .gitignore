# 1. 绝对机密（绝不能上传！）
.env

# 2. Python 运行时的垃圾缓存（传上去会引起冲突）
__pycache__/
*.pyc
*.pyo

# 3. Pytest 的测试缓存
.pytest_cache/

# 4. Allure 生成的本地报告临时数据（每次运行都会变，没必要上传）
temps/
reports/

# 5. 虚拟环境（如果你是在项目目录下建的 venv）
venv/
env/

#6.日志文件
logs/