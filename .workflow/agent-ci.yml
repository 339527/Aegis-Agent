version: '1.0'
name: agent-ci
displayName: agent-ci
triggers:
  trigger: auto
  push:
    branches:
      include:
        - master
stages:
  - name: run-agent-test
    displayName: AgentTest
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@python
        name: run-python
        displayName: RunPython
        pythonVersion: '3.10'
        commands:
          - echo "🚀 Booting Agent CI..."
          - pip install -r requirements.txt
          - export RUN_ENV=ci
          - python run.py
        env:
          ZHIPU_API_KEY: ${ZHIPU_API_KEY}
          RUN_ENV: ci